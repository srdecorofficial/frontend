rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All direct client access is denied.
    // All reads and writes must go through the backend Express API
    // which uses the Firebase Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
